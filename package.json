{
  "name": "clientcontroller",
  "private": true,
  "version": "0.1.2",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc --noEmit && vite build",
    "sync:workflow-runtime": "node scripts/sync-workflow-runtime.mjs",
    "prepare:workflow-runtime": "npm run sync:workflow-runtime && node scripts/install-workflow-runtime.mjs && node scripts/package-workflow-runtime.mjs",
    "install:workflow-runtime": "npm run prepare:workflow-runtime",
    "preview": "vite preview",
    "tauri": "tauri"
  },
  "dependencies": {
    "vue": "^3.5.13",
    "@tauri-apps/api": "^2",
    "@tauri-apps/plugin-opener": "^2"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.2.1",
    "typescript": "~5.6.2",
    "vite": "^6.0.3",
    "vue-tsc": "^2.1.10",
    "@tauri-apps/cli": "^2"
  }
}
