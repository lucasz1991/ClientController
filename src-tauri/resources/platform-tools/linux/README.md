# Linux ADB Dateien

Lege hier die Linux `platform-tools` Dateien ab:

- `adb`
- (optional weitere benötigte Tools aus `platform-tools`)

Pfad:

`src-tauri/resources/platform-tools/linux/`

Wichtig:

- `adb` muss ausführbar sein (`chmod +x adb`), sonst kann die App es nicht starten.
