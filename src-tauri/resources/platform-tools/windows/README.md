# Windows ADB Dateien

Lege hier die Windows `platform-tools` Dateien ab:

- `adb.exe`
- `AdbWinApi.dll`
- `AdbWinUsbApi.dll`

Pfad:

`src-tauri/resources/platform-tools/windows/`

Diese Dateien werden durch Tauri-Bundling mit ausgeliefert.
